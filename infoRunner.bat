if runner.Function = local 
then askForChild = parent.WiFi = infoRunner
     else.ifFindableWiFi = true 
     then searchForParent = WiFi.findableMode = true 

     else (ifFalse) = not 
     then find = findableMode = supportedByTheClient.source = true 

    or false = setTrueEverytime.unBlockUser = setMode.true (activeTrue(activateMode))

    goUp.checkForChild = if true 
    then askForWiFi.findable 

    if goUp (checkUp(searchForWiFi)) = true then endInstant.end
    end)