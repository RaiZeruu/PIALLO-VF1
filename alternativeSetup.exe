if print (if (pressed) then
thenFindFunction.true = run.PialloSoftware = local.Function = true 
if goUp (check) then 
   print ("Starting PIALLO Software, please be patient.") if goSide

   if.True then 
      (findFunction(or(endInstant.goDown)))

      end)) = function.FunctioningByTheServer.false = falseFunction then 

      runFor (with) 
      print ("An issue was found while trying to run the PIALLO Software, please try and alternative setup.")

else.runWith (for) then 

findFunction.WiFi = runSoftware.PIALLO 

then = findableMode.true = if (true) then activateMode.findableMode 
    function.findableWiFi = true 

    then else = run (runSoftware)
    (or) checkForward = (endInstant.end) goDown 
    checkFor.goDown

    end)